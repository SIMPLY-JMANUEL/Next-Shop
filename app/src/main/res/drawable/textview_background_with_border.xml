<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/holo_blue_light" /> <!-- Color de fondo -->

    <stroke
        android:width="2dp" <!-- Grosor del borde -->
    android:color="@android:color/black" /> <!-- Color del borde -->

    <corners android:radius="8dp" /> <!-- Redondeo de los bordes (opcional) -->
</shape>
